{
  "name": "node-server-testing",
  "version": "1.0.0",
  "description": "Node Server Testing Challenge",
  "scripts": {
    "test": "jest --watch",
    "start": "node index.js",
    "server": "nodemon index.js",
    "migrate": "knex migrate:latest",
    "rollback": "knex migrate:rollback",
    "seed": "knex seed:run",
    "reset": "npm run rollback && rpm run migrate && npm run seed"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LambdaSchool/node-server-testing.git"
  },
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^26.6.3",
    "knex-cleaner": "^1.3.1",
    "nodemon": "^2.0.12",
    "supertest": "^6.0.1"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "knex": "^0.21.21",
    "sqlite3": "^5.0.2"
  }
}
